@model AgriculturalForum.Web.ModelViews.ChangePasswordViewModel
@{
    ViewBag.Title = _localization.Getkey("ChangePassword");
}

<section class="breadcrumbs">
    <div class="container">
        <div class="d-flex justify-content-between align-items-center">
            <h2>@ViewBag.Title</h2>
            <ol>
                <li><a href="~/Account/Detail">Tài khoản của bạn</a></li>
                <li>@ViewBag.Title</li>
            </ol>
        </div>
    </div>
</section>

<section id="blog" class="blog">
    <div class="container" data-aos="fade-up">

        <div class="row">
            <div class="col-lg-4">

                @await Html.PartialAsync("_SidebarPartialView.cshtml")

            </div>

            <div class="col-lg-8 entries">

                <article class="entry">

                    <h2 class="entry-title">
                       @_localization.Getkey("ChangePassword")
                    </h2>

                    <div class="entry-content">
                        <form asp-controller="Account" asp-action="ChangePassword" method="post">
                            <div class="mb-3">
                                <label for="passwordNow" class="form-label fw-bold">@_localization.Getkey("OldPassword")</label>
                                <input asp-for="PasswordNow" type="password" class="form-control" id="passwordNow">
                            </div>
                            <div class="mb-3">
                                <label for="password" class="form-label fw-bold">@_localization.Getkey("NewPassword")</label>
                                <input asp-for="Password" type="password" class="form-control" id="password">
                            </div>
                            <div class="mb-3">
                                <label for="confirmPassword" class="form-label fw-bold">@_localization.Getkey("ConfirmPassword")</label>
                                <input asp-for="ConfirmPassword" type="password" class="form-control" id="confirmPassword">
                            </div>
                            <div class="mb-3">
                                <button class="btn btn-primary" type="submit">@_localization.Getkey("Submit")</button>
                            </div>
                        </form>
                    </div>

                </article><!-- End blog entry -->

            </div><!-- End blog entries list -->
        </div>
    </div>
</section>
